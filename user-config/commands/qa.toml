description = "Trigger a structured QA review cycle."
prompt = """
ACTIVATE SKILL: qa-verifier
INSTRUCTION:
1. Read `SESSION_PLAN.json`.
2. Find all tasks with `status: completed`.
3. For each task:
   - Run `search_file_content` to check for security issues/TODOs.
   - Run `run_shell_command` to execute project tests (prefer sandbox).
   - Verify compliance with any active Policies.
4. Update status to `qa_passed` or `pending` (if rejected).
5. Sync `SESSION_PLAN.md`.
"""
