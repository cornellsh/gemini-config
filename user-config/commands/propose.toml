description = "Translate intent into an OpenSpec proposal and initialize the session plan."
prompt = """
ACTIVATE SKILL: product-strategist
INSTRUCTION:
1. Gather the requirements for "{{args}}".
2. Draft a new OpenSpec proposal in openspec/changes/ following the strict formatting in openspec/AGENTS.md.
3. Ensure every requirement has at least one "#### Scenario:" block with WHEN/THEN formatting.
4. Run "openspec validate [change-id] --strict --no-interactive" to verify the proposal.

ACTIVATE SKILL: orchestrator
INSTRUCTION:
1. Initialize the session plan for this project if it is missing (Self-Healing).
2. Read the tasks generated by the product-strategist.
3. Initialize the execution queue in SESSION_PLAN.json and sync to SESSION_PLAN.md.
"""
