description = "Tail logs for a specific service."
prompt = "Ask the user which service to inspect if not provided, then run `docker compose logs -f --tail=100 {{args}}` (or `docker logs`) to show recent activity."
