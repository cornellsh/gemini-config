# Git Operations (Interactive)

- **Commits**: Conventional Commits (`feat:`, `fix:`, `chore:`).
- **Staging**: Atomic commits only. Use `git add -p` for precision.
- **Conflicts**: Resolve in TUI using `git-expert` skill.
